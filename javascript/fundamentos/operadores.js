//OPERADORES ARITMÉTICOS
const soma = a + b 
const subtracao = a - b 
const multiplicacao = a * b
const divisao = a / b
const modulo = a % 2
const exponenciacao = a ** b  

//OPERADORES DE ATRIBUIÇÃO
b += 10 // b = b + 10 <= soma
b -= 4 // b = b - 4 <= subtração 
b *= 2 // b = b * 2 <= multiplicação
b /= 2 // b = b / 2 <= divisão
b %= 2 // b = b % 2 <= módulo
b **= 2 // b = b ** 2 <= exponenciacao

//OPERADORES UNARIOS
//atuam em cima de apenas uma variável

num++
num--
++num  
--num

//OPERADORES TERNARIOS
//atuam com tres operandos
//especie de if / else
nota >= 7 ? 'Aprovado' : 'Reprovado'

//OPERADOR DESTRUCTURING (DESESTRUTURAÇÃO)

