//! INTRODUÇÃO
//* Função é basicamente o que move o JS
//* Podemos usar ela como parametro para outra função, armazena-la em variaveis, arrays, obj etc
// ? Não podemos esquecer que o parâmetro e o retorno de uma função são opcionais 

/*
    //* Declarando função de forma literal
        function fun1(parameters){
	        <sentença de código>
	        return
        }
    //* Armazenando uma função em uma variável
        const fun2 = function () {
	        <sentença de código>
	    return
        }
    //* Armazenando uma função em um atributo de um objeto de forma dinamica
        const obj = {}
        obj.falar = function () {return 'Opa'}
   
*/



